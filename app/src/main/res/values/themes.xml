<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Theme.Comp3074project" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Brand colors -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>

        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorSecondaryVariant">@color/colorSecondaryVariant</item>
        <item name="colorOnSecondary">@color/colorOnSecondary</item>

        <!-- Background & surface -->
        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="colorSurface">@color/colorSurface</item>
        <item name="colorOnSurface">@color/colorOnBackground</item>
        <item name="colorOnBackground">@color/colorOnBackground</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
    </style>

    <!-- ✅ “Add New” Button (Soft Green) -->
    <style name="Widget.MyApp.Button.Primary" parent="Widget.MaterialComponents.Button">
        <item name="cornerRadius">12dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">16sp</item>
        <item name="backgroundTint">@color/colorSecondary</item>        <!-- soft green -->
        <item name="android:textColor">@color/colorOnSecondary</item>   <!-- navy -->
        <item name="iconTint">@color/colorOnSecondary</item>
        <item name="rippleColor">@color/colorSecondaryVariant</item>    <!-- soft blue ripple -->
    </style>

    <!-- ✅ “View All” Button (Soft Blue) -->
    <style name="Widget.MyApp.Button.Secondary" parent="Widget.MaterialComponents.Button">
        <item name="cornerRadius">12dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">16sp</item>
        <item name="backgroundTint">@color/colorSecondaryVariant</item> <!-- soft blue -->
        <item name="android:textColor">@color/colorOnSecondary</item>   <!-- navy -->
        <item name="iconTint">@color/colorOnSecondary</item>
        <item name="rippleColor">@color/colorSecondary</item>           <!-- soft green ripple -->
    </style>

</resources>
